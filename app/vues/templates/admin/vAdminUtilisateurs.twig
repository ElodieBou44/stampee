<script>
	history.pushState({}, '', 'admin?entite=utilisateur')
</script>
<section>
	<h1 class="admin__h1">{{ titre|upper }}</h1>
	<p class="messageRetourAction {{ classRetour }}">{{ messageRetourAction }}&nbsp;</p>
	<div class="g utilisateurs">
		<div class="t">Id</div>
		<div class="t">Nom, prénom</div>
		<div class="t">Courriel</div>
		<div class="t">Profil</div>
		<div class="t">Statut</div>
		<div class="t">Actions</div>
		{% for utilisateur in utilisateurs %}
			<div class="l">
				<div class="c dr">{{ utilisateur.utilisateur_id }}</div>
				<div class="c   ">{{ utilisateur.utilisateur_nom }},
					{{ utilisateur.utilisateur_prenom }}</div>
				<div class="c   ">{{ utilisateur.utilisateur_courriel }}</div>
				<div class="c   ">
					{% if utilisateur.utilisateur_profil_id == 1 %}
						administrateur
					{% else %}
						membre
					{% endif %}
				</div>
				<div class="c   ">{{ utilisateur.utilisateur_statut }}</div>
				<div class="c   ">
					<a class="admin__a" href="admin?entite=utilisateur&action=m&utilisateur_id={{ utilisateur.utilisateur_id }}">modifier</a>
					<a class="admin__a" href="admin?entite=utilisateur&action=s&utilisateur_id={{ utilisateur.utilisateur_id }}">supprimer</a>
				</div>
			</div>
		{% endfor %}
	</div>
</section>
